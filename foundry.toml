[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
# this allows foundry to run cmds directly on our machine
ffi = true
# See more config options https://github.com/foundry-rs/foundry/tree/master/config
